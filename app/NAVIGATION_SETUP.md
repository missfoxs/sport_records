# Navigation 设置说明

## ✅ 已完成

1. ✅ 创建了两个空白页面：
   - `src/screens/HomeScreen.tsx` - 首页
   - `src/screens/StatisticsScreen.tsx` - 统计页

2. ✅ 创建了底部 Tab 导航：
   - `src/navigation/TabNavigator.tsx` - Tab 导航配置

3. ✅ 更新了 App.tsx：
   - 集成了 NavigationContainer
   - 添加了 GestureHandlerRootView
   - 配置了深色主题样式

4. ✅ 更新了入口文件：
   - `index.js` - 添加了 gesture-handler 初始化

## 📦 需要安装的依赖

由于权限问题，请手动运行以下命令安装依赖：

```bash
cd /Users/zhouming.wang/workspace/rn/sport_records/app
pnpm install
```

或者如果 pnpm 仍有问题，可以使用：

```bash
npm install
# 或
yarn install
```

## 🔧 iOS 配置

安装依赖后，需要运行：

```bash
cd ios
pod install
cd ..
```

## 🚀 运行应用

安装完依赖后：

```bash
# Android
pnpm android

# iOS
pnpm ios
```

## 📱 功能说明

- **底部 Tab 导航**：两个标签页（首页、统计）
- **深色主题**：使用了设计文档中的配色方案（#0a0a0a 背景，#00ff88 激活色）
- **SafeArea 支持**：两个页面都使用了 SafeAreaView

## 🎨 样式特点

- Tab 栏：深色背景 (#1a1a1a)，绿色激活色 (#00ff88)
- 页面背景：深色 (#0a0a0a)
- 临时图标：使用 emoji（🏠 📊），后续可以替换为 react-native-vector-icons

## 📝 下一步

根据设计文档，接下来可以：
1. 添加状态管理（Zustand）
2. 实现今日任务列表
3. 添加统计图表
4. 实现添加运动功能
